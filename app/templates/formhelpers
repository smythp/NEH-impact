{% macro render_field(field) %}
  <p>
    {{ field.label }}
    {{ field(**kwargs)|safe }}
</p>
  {% if field.errors %}

    {% for error in field.errors %}
<p>* {{ error }}</p>
    {% endfor %}

  {% endif %}

{% endmacro %}
